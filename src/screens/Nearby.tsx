import React from "react"
import { ScrollView, Text } from "react-native"

export const NearbyScreen = () => {
    return (
        <>
            <ScrollView>
                <Text> Nearby !</Text>
            </ScrollView>
        </>
    )
}