import React from "react"
import { ScrollView, Text } from "react-native"

export const HomeScreen = () => (
    <>
        <ScrollView>
            <Text> Home !</Text>
        </ScrollView>
    </>
)