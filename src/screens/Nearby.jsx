import React from "react"
import { ScrollView, Text } from "react-native"

export const NearbyScreen = () => (
    <>
        <ScrollView>
            <Text> Nearby !</Text>
        </ScrollView>
    </>
)